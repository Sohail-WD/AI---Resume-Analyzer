kind = "web"
previewPath = "/"
title = "AI Resume + Portfolio Analyzer"
version = "1.0.0"
id = "artifacts/ai-analyzer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24697

[services.development]
run = "pnpm --filter @workspace/ai-analyzer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-analyzer", "run", "build" ]
publicDir = "artifacts/ai-analyzer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24697"
BASE_PATH = "/"
